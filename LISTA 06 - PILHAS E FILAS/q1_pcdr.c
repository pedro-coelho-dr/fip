#include <stdio.h>
#include <stdlib.h>
 
int main() {
 
    int n, x, y;
    while(scanf("%d", &n) != EOF){
        for (int i=0; i<n; i++){
            scanf("%d %d", &x, &y);

        }
    }
 
    return 0;
}